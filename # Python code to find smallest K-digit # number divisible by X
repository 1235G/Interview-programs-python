

def answer(X, K):
	
	MIN = pow(10, K-1)
	
	if(MIN%X == 0):
		return (MIN)
	
	else:
		return ((MIN + X) - ((MIN + X) % X))
	

X = int(input("enter the integer))
K = int(input("enter the integer))

print(answer(X, K));
